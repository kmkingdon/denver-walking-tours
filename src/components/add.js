import React, { Component } from 'react';
import styled from 'styled-components';
import Header from '../containers/header';

class Add extends Component {


  render() {
    return (
      <div>
        <Header />
        <h2> You are now on the add page</h2>
      </div>
    )
  }
}

export default Add;
