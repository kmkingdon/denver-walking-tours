import React, { Component } from 'react';
import styled from 'styled-components';
import Header from '../containers/header';

class Start extends Component {


  render() {
    return (
      <div>
        <Header />
        <h2> You are now on the start page</h2>
      </div>
    )
  }
}

export default Start;
